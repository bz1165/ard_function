这说明问题已经缩小了：

**`EmployeeName` 这个字段本身大概率没问题。**
`First(EmployeeScopes).EmployeeName` 能拿到值，说明 Power Apps 已经认到这列了。

所以 `SortByColumns(...)` 报 **无效参数**，更常见的是下面这几种情况：

## 最可能的 3 个原因

### 1. 出问题的不是 `EmployeeName`，而是后面的 `TargetTA` 或 `Environment`

`SortByColumns` 是按参数顺序解析的。只要后面某一列有问题，整个函数都会报“无效参数”。
Microsoft 官方文档说明，`SortByColumns` 的列名要作为**字符串**传入，而且如果有多列，除最后一列外都要配对应的 `SortOrder`。([Microsoft Learn][1])

### 2. `TargetTA` / `Environment` 在 SharePoint 里可能不是“纯文本”，而是复杂类型

这很常见。比如如果它们被建成了：

* Choice
* Lookup
* Person

那 `First(EmployeeScopes).TargetTA` 看起来“有东西”，但它拿到的可能是一个 **record**，不是纯文本。
而 `SortByColumns` 更适合按直接列名排序；如果字段是复杂类型，往往更稳的是改用 `Sort(...)`，按一个公式去排序。微软文档也明确说了：`Sort` 是按公式排序，而这个公式必须返回 **number / string / Boolean**，不能返回 table 或 record。([Microsoft Learn][1])

### 3. 你当前排序的并不是 `EmployeeScopes`，而是一个经过处理的表

如果你现在真正写的是：

```powerapps
SortByColumns(colManagerScopes, ...)
```

那就要确认 `colManagerScopes` 里是不是还保留了：

* `EmployeeName`
* `TargetTA`
* `Environment`

因为 Power Fx 里的表经过 `ShowColumns / AddColumns / DropColumns` 后，会变成一张新的表。
你在 `EmployeeScopes` 上能 `First(...).EmployeeName`，不代表 `colManagerScopes` 里也一定有这个列。

---

# 现在最稳的排查顺序

## 先只测一列

先试最小版本：

```powerapps id="d9l1z9"
SortByColumns(
    EmployeeScopes,
    "EmployeeName",
    SortOrder.Ascending
)
```

### 结果判断

* 如果这一句 **能过**，说明 `EmployeeName` 没问题
* 如果这一句 **也不行**，那就是数据源 schema 还没完全刷新好

---

## 再逐列加回去

第二步试：

```powerapps id="8szcbi"
SortByColumns(
    EmployeeScopes,
    "EmployeeName",
    SortOrder.Ascending,
    "TargetTA",
    SortOrder.Ascending
)
```

第三步再加 `Environment`。

这样你马上就能知道：

* 是 `TargetTA` 出问题
* 还是 `Environment` 出问题

---

# 我更推荐你的修法：改用 `Sort`，不要死磕 `SortByColumns`

微软官方文档明确给了这种写法：
要先按一列、再按另一列排序，可以 **嵌套 `Sort`**。([Microsoft Learn][1])

## 如果这三列都是纯文本

直接改成：

```powerapps id="h1hos9"
Sort(
    Sort(
        Sort(
            EmployeeScopes,
            Environment,
            SortOrder.Ascending
        ),
        TargetTA,
        SortOrder.Ascending
    ),
    EmployeeName,
    SortOrder.Ascending
)
```

## 如果你实际是在排序 `colManagerScopes`

那就写成：

```powerapps id="omgl6i"
Sort(
    Sort(
        Sort(
            colManagerScopes,
            Environment,
            SortOrder.Ascending
        ),
        TargetTA,
        SortOrder.Ascending
    ),
    EmployeeName,
    SortOrder.Ascending
)
```

---

# 如果 `TargetTA` / `Environment` 是 Choice 列

那就不要直接写列名，而要排它的 `.Value`。
因为 `Sort` 的公式必须返回字符串/数字/布尔，不可以返回 record。([Microsoft Learn][1])

## 这时写成：

```powerapps id="d795x5"
Sort(
    Sort(
        Sort(
            colManagerScopes,
            Environment.Value,
            SortOrder.Ascending
        ),
        TargetTA.Value,
        SortOrder.Ascending
    ),
    EmployeeName,
    SortOrder.Ascending
)
```

如果 `EmployeeName` 也是复杂类型，就同理看是不是要 `.DisplayName` 或别的子字段，不过按你现在的描述，`EmployeeName` 大概率是纯文本。

---

# 还有一个要注意的官方细节

如果 SharePoint / Excel 的列名里有空格，`SortByColumns` 里要写成 `_x0020_` 形式，比如：

```powerapps id="4wl5uy"
"Column_x0020_Name"
```

微软文档明确提到了这个规则。([Microsoft Learn][1])

虽然你这几个列名看起来没空格，但这个规则你后面会用到。

---

# 我建议你现在直接这样做

## 不要继续卡在 `SortByColumns`

先把你原来的：

```powerapps id="j83jhy"
SortByColumns(
    ...,
    "EmployeeName",
    SortOrder.Ascending,
    "TargetTA",
    SortOrder.Ascending,
    "Environment",
    SortOrder.Ascending
)
```

改成嵌套 `Sort`。

### 先试文本版：

```powerapps id="e4sl0x"
Sort(
    Sort(
        Sort(
            Filter(
                colManagerScopes,
                Lower(Trim(ManagerEmail)) = varUserEmail
            ),
            Environment,
            SortOrder.Ascending
        ),
        TargetTA,
        SortOrder.Ascending
    ),
    EmployeeName,
    SortOrder.Ascending
)
```

### 如果报 `TargetTA` / `Environment` 类型不对，再改成 `.Value` 版：

```powerapps id="fkcibk"
Sort(
    Sort(
        Sort(
            Filter(
                colManagerScopes,
                Lower(Trim(ManagerEmail)) = varUserEmail
            ),
            Environment.Value,
            SortOrder.Ascending
        ),
        TargetTA.Value,
        SortOrder.Ascending
    ),
    EmployeeName,
    SortOrder.Ascending
)
```

---

# 一句话判断

**`First(...).EmployeeName` 能读到，不代表 `SortByColumns` 里的所有列都没问题。**
真正更稳的做法是：

* 先逐列测试是哪一列坏了
* 然后直接改用嵌套 `Sort`
* 如果列是 Choice/Lookup，就排序它的 `.Value`

把你现在报错的那条完整 `SortByColumns(...)` 公式贴给我，我可以直接帮你改成你当前这个数据源能跑的版本。

[1]: https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-sort "Sort and SortByColumns functions - Power Platform | Microsoft Learn"
